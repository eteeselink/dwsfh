.Button
    foreground-background($main-color)
    height 32px
    border 0px
    border-top-right-radius 4px
    padding 6px 10px
    font-style normal

.Button[disabled]
    foreground-background(saturation($main-color, 0))
